version: "1"
project:
  id: service-c
  name: Service C
  description: Sub-project with no readability block, so it inherits the root's
structure:
  adr_dir: adr
  rfc_dir: rfcs
  memo_dir: memos
  document_folders:
    - adr
security:
  allow_external_paths: false
