# Glossary

Shared reference material that lives next to the site checkout rather than
inside it, so a link to it from `site/` leaves the repository root the run was
pointed at. It is not scanned: it is outside `--repo-root`.

## Terms

Nothing here matters except that the file exists on disk.
