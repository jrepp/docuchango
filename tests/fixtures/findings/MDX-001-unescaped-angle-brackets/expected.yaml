summary: >-
  An ADR with a bare `<token>` placeholder in prose and bare `<`/`>` before
  numbers in a bullet list. Which of the two shapes is reported depends on
  the MDX check's current rules, so only the shared "Unescaped" wording is
  asserted here; tighten it once the check settles.
messages:
  - "Unescaped"
# MDX-010 (planned) would escape these automatically.
fixed: false
dry_run_exit_code: 1
exit_code: 1
