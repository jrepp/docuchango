summary: >-
  An ADR whose `project_id` is not the `project.id` of the governing
  docs-project.yaml. The value is not the `my-project` placeholder, so it may
  be deliberate: FM-010 reports it and never rewrites it.
messages:
  - "project_id"
  - "fixture-project"
fixed: false
dry_run_exit_code: 1
exit_code: 1
