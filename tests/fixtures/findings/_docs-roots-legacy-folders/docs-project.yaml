version: "1"
project:
  id: fixture-project
  name: Fixture Project
  description: Legacy document folders that live under a configured docs root
structure:
  docs_roots:
    - docs-cms
  adr_dir: adr
  rfc_dir: rfcs
  memo_dir: memos
  document_folders:
    - adr
    - rfcs
    - memos
security:
  allow_external_paths: false
readability:
  enabled: false
